import DeviceAnimation from "../../../short motion graphic/DeviceAnimation";

export const metadata = {
  title: "Device Animation | ZA Cleaning",
  robots: { index: false },
};

export default function AnimationPage() {
  return <DeviceAnimation />;
}
